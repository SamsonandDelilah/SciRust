[package]
name = "symbolic"
version = "0.1.0"
edition = "2024"

[features]
default = ["native"]
native = []
python-bindings = ["dep:pyo3"]

[dependencies]
pyo3 = { version = "0.22", features = ["auto-initialize"], optional = true }
